# Dependencies
node_modules/
*/node_modules/

# Environment variables
.env
.env.local
.env.*.local
.env.production
frontend/.env.production
frontend/.env.vercel.production
frontend/.env.production.local
server/.env

# Logs
logs
*.log
npm-debug.log*

# Build outputs
dist/
build/
*/dist/
*/build/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
# Personal VS Code workspace files (per-developer paths and folder layout)
*.code-workspace

# Agent/tooling scratch
.claude/worktrees/
.superpowers/
# Codex (Duex) local config
.codex/
# Playwright MCP console/network dumps from browser-driven testing
.playwright-mcp/

# Stray asset dropped at the repo root. Brand assets live with the frontend,
# not here — ignored rather than committed so it does not become the canonical
# copy by accident.
/VES-LOGO black.png

# OS
.DS_Store
Thumbs.db

# Temporary files
tmp/
temp/
*.tmp
.vercel

# Supabase CLI local state (contains connection strings, project refs)
supabase/.temp/
supabase/.branches/

# graphify knowledge graph — generated artifacts, rebuilt by the post-commit
# hook. ~20MB incl. a 2000-file Obsidian vault and dated graph backups.
# Regenerate with `graphify update .` rather than tracking it.
graphify-out/

# Local-only: contains customer PII, never commit
server/scripts/backup-legacy-class-allocations.json
.date-repair-backups/
